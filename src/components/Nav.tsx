import styled from 'styled-components'

const Nav = styled.ul`
  /* Use flex utilities to control alignments */
  display: flex;
  flex-direction: column;

  list-style: none;
  height: 100%;
  margin: 0 0 0 auto;
`

export default Nav
